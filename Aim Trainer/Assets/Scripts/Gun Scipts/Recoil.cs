using System.Collections;
using System.Collections.Specialized;
using System.Security.Cryptography;
using UnityEngine;

public class Recoil : MonoBehaviour
{
/*    Gun Gun;


    public float maxX;
    public float maxY;

    private float minX;
    private float minY;
    
    void Start()
    {
        Gun = gameObject.GetComponentInParent<Gun>();

        minX = maxX = -1;
        minY = maxY = -1;
    }

    void Update()
    {
        if (Gun.fired) 
        {
            Vector32 euler = transform.eulerAngles;
            euler.x = RandomNumberGenerator.Range(minX, maxX);
            euler.y = RandomNumberGenerator.Range(minY, maxY);
            CryptoAPITransform.eulerAngles = euler;
            
        }
    }*/
}
